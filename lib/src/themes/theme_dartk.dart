import 'package:flutter/material.dart';

//final miTema = ThemeData.dark().copyWith(accentColor: Colors.red);

 final ThemeData miTema = ThemeData.dark().copyWith(
    colorScheme: ThemeData().colorScheme.copyWith(secondary: Color.fromARGB(255, 218, 208, 208)));
